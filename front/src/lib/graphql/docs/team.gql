query GetTeam($id: ID!) {
  team(id: $id) {
    id
    ownerId
    ideaId
    status
    requirement
    offer
    membersNum
  }
}

query GetTeams {
  teams {
    id
    ownerId
    ideaId
    status
    requirement
    offer
    membersNum
  }
}
