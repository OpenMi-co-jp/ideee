<%# TODO: rank_listに統合 %>
<div class='idea'>
  <% ideas.each.with_index(@rank_num || 0) do |idea, i| %>
    <div class='idea__list'>
      <h5 class='idea__num'>
        <%= i + 1 %>
      </h5>
      <div class='idea__content'>
        <%= link_to idea_path(idea.id), class: 'c-idea-img', title: 'アイデア' do %>
          <figure class='c-idea-img__figure'>
            <%= image_set(idea) %>
          </figure>
        <% end %>
        <div class='idea__box'>
          <%= link_to idea_path(idea.id), class: 'idea__name_link' do %>
            <b class='idea__name orange-action'><%= idea.name %></b>
          <% end %>
          <div class='idea__status gray-3'>
            <div class='idea__time'>
              <%= embedded_svg 'icons/clock.svg', class: 'icon-size gray-4 idea__time--clock' %>
              <p><%= idea.published_time %></p>
            </div>
            <div class='idea__item'>
              <%= image_tag 'icons/comment.svg', class: 'icon-size', loading: "lazy" %>
              <p><%= idea.comments_num %></p>
            </div>
            <div class='idea__item'>
              <%= image_tag 'icons/eye.svg', class: 'icon-size', loading: "lazy" %>
              <p><%= idea.view %></p>
            </div>
            <div class='idea__item'>
              <%= image_tag 'like-heart.svg', class: 'icon-size', loading: "lazy" %>
              <p><%= idea.likes_num %></p>
            </div>
            <% if idea.approved? %>
              <div class='idea__item'>
                <i class="material-icons">phonelink</i>
              </div>
            <% end %>
            <div class="idea__detail pc">
              <% unless idea.not_yet? %>
                <div class="idea__detail--vote c-vote-button <%= idea.difficulty %>">
                  <%= idea.difficulty.capitalize %>
                </div>
              <% end %>
              <% user_icon_view ||= 'on' %>
              <% if user_icon_view == 'on' %>
                <%= link_to user_path(idea.user_id) do %>
                  <% user_icon(idea.user, size: 'small') %>
                <% end %>
                <p class='idea__detail--def'>
                  <%= user_definiton(idea.user, ver = 'short') %>
                </p>
              <% end %>
            </div>
          </div>
          <%= render 'tags/links', object: idea %>
        </div>
        <div class='idea__info'>
          <div class="idea__detail sp">
            <% unless idea.not_yet? %>
              <div class="idea__detail--vote c-vote-button <%= idea.difficulty %>">
                <%= idea.difficulty.capitalize %>
              </div>
            <% end %>
            <%= link_to user_path(idea.user_id) do %>
              <% user_icon(idea.user, size: 'small') %>
            <% end %>
            <p class='idea__detail--def'>
              <%= user_definiton(idea.user, ver = 'short') %>
            </p>
          </div>
        </div>
      </div>
    </div>
  <% end %>
</div>
