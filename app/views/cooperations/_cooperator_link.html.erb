<% if idea.cooperation_ongoing? %>
  <% if current_user&.cooperation_joined?(idea) %>
    <%= link_to(
      t('default.cooperation.cancel'),
      '#modal_cooperation_cancel',
      data: { turbolinks: false },
      class: 'waves-effect waves-light btn gray-3-back modal-trigger',
      id: 'modal-trigger-cooperation-cancel',
    )%>
    <%= render 'cooperations/modal',
      modal_id: 'modal_cooperation_cancel',
      heading: 'この開発への参加を辞退しますか？',
      name: t('default.cooperation.cancel'),
      path: cooperation_path(idea_id: idea),
      method: :delete,
      message: nil
    %>
  <% else %>
    <%= link_to(
      t('default.cooperation.join'),
      '#modal_cooperation_join',
      data: { turbolinks: false },
      class: 'btn orange-action-back modal-trigger',
      id: 'modal-trigger-cooperation-cancel',
    )%>
    <%= render 'cooperations/modal',
      modal_id: 'modal_cooperation_cancel',
      heading: '協働開発への参加を希望しますか？',
      name: t('default.cooperation.join'),
      path: cooperations_path(idea_id: idea),
      method: :post,
      message: nil
    %>
  <% end %>
<% end %>
