overwrite: true
schema: http://localhost:3010/graphql
documents: src/lib/graphql/docs/*.gql
generates:
  src/lib/generated/client.ts:
    plugins:
      - typescript
      - typescript-operations
      - typescript-react-apollo
