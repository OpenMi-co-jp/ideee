<%= form_with url: search_ideas_path, method: :get, id: 'sort_form' do |f| %>
  <%= f.select :selected_sort, options_for_select(sort_hash, selected: @sort  ) %>
  <%= f.hidden_field :keyword, value: @keyword %>
<% end %>
