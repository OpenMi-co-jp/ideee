<div class='p-comment-column-inside'>
  <div class="flex">
    <% unless owned %>
      <div class='p-comment-img gray-2'>
        <%= link_to user_path(comment.user_id) do %>
          <%= user_icon(User.find(comment.user_id)) %>
        <% end %>
      </div>
    <% end %>
    <div class="p-comment-info <%= 'white-text' if owned %>">
      <div class='p-comment-description'>
        <%== simple_format(text_url_to_link(comment.description), {}, sanitize: false) %>
      </div>
      <div class="p-comment-options">
        <div class="date-small pt-2 <%= 'gray-3' unless owned %>">
          <%= l comment.created_at, format: :long %>
        </div>
        <% if owned %>
          <%= render 'comments/icons', comment: comment %>
        <% end %>
      </div>
    </div>
  </div>
</div>
