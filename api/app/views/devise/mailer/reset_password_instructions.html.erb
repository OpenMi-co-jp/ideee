<p>※このメールは自動送信メールです。</p>

<p>ideee事務局です。</p>
<p><%= @resource.email %> さん</p>

<p>パスワードの変更が行われました！</p>

<p><%= link_to 'パスワードの変更', edit_user_password_url(@resource, reset_password_token: @token) %></p>

<p>もしこのメールに認識がない場合は無視してください。</p>
<p>上記のURLをクリックしてアクセスされない限り、パスワードのリセットは行われません。</p>

<%= render 'underwriting' %>
